import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="loader">
      <div class="loader">Loading...</div>
    </div>
  );
};

export default Spinner;
